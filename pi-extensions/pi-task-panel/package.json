{
  "name": "pi-task-panel",
  "version": "0.1.0",
  "description": "Persistent Claude-style tasks panel for Pi with slash commands and a structured tasks_write tool.",
  "license": "MIT",
  "keywords": [
    "pi-package",
    "pi",
    "coding-agent",
    "tasks"
  ],
  "pi": {
    "extensions": [
      "./extensions/task-panel.ts"
    ]
  },
  "vstack": {
    "extensionManager": {
      "displayName": "Task Panel",
      "settings": [
        {
          "key": "enabled",
          "label": "Enable task panel",
          "description": "Register tasks_write, /tasks, task panel widgets, reminders, and shortcuts.",
          "type": "boolean",
          "default": true,
          "category": "General",
          "apply": "reload",
          "requiresReload": true
        },
        {
          "key": "panelDefaultState",
          "label": "Default panel state",
          "description": "Panel state when tasks first appear.",
          "type": "enum",
          "enumValues": [
            "hidden",
            "compact",
            "expanded"
          ],
          "default": "compact",
          "category": "Panel",
          "apply": "live"
        },
        {
          "key": "takeoverCtrlT",
          "label": "Use Ctrl+T for tasks",
          "description": "Register Ctrl+T for task panel visibility. Pi normally uses Ctrl+T for thinking visibility, so this requires reload and explicit opt-in.",
          "type": "boolean",
          "default": false,
          "category": "Keyboard",
          "apply": "reload",
          "requiresReload": true
        },
        {
          "key": "alternateShortcut",
          "label": "Alternate shortcut",
          "description": "Always-available task panel shortcut when Ctrl+T is not taken over. Cycles hidden → show compact → show all.",
          "type": "string",
          "default": "alt+t",
          "category": "Keyboard",
          "apply": "reload",
          "requiresReload": true
        },
        {
          "key": "maxCompactTasks",
          "label": "Compact task count",
          "description": "Maximum tasks shown in compact mode.",
          "type": "number",
          "default": 4,
          "category": "Panel",
          "apply": "live"
        },
        {
          "key": "showNotesInExpanded",
          "label": "Show active notes",
          "description": "Show notes for the active task in expanded panel mode.",
          "type": "boolean",
          "default": true,
          "category": "Panel",
          "apply": "live"
        },
        {
          "key": "autoShowOnFirstTask",
          "label": "Auto-show on first task",
          "description": "Show the compact panel automatically when the first task is added.",
          "type": "boolean",
          "default": true,
          "category": "Panel",
          "apply": "live"
        },
        {
          "key": "compactToolOutput",
          "label": "Compact tasks_write output",
          "description": "Render tasks_write results as a single inline status row instead of Pi's padded tool box.",
          "type": "boolean",
          "default": true,
          "category": "Tool Output",
          "apply": "reload",
          "requiresReload": true
        },
        {
          "key": "showWorkflowReminder",
          "label": "Task workflow reminders",
          "description": "Inject task workflow reminders and hidden current task context so the agent reconciles active/done/dropped/follow-up tasks before final replies.",
          "type": "boolean",
          "default": true,
          "category": "Reminder",
          "apply": "live"
        },
        {
          "key": "showIncompleteReminder",
          "label": "Incomplete-task reminders",
          "description": "Show a subtle reminder when an agent turn ends with incomplete tasks.",
          "type": "boolean",
          "default": true,
          "category": "Reminder",
          "apply": "live"
        }
      ]
    }
  },
  "peerDependencies": {
    "@mariozechner/pi-ai": "*",
    "@mariozechner/pi-coding-agent": "*",
    "@mariozechner/pi-tui": "*",
    "typebox": "*"
  },
  "files": [
    "extensions/",
    "assets/",
    "README.md",
    "package.json"
  ]
}
